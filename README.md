# ♾️ EJInfiniteCarousel

A highly customizable UICollectionView-based view for seamless, bidirectional infinite scrolling on iOS.

## ✨ Key Features

- [x] **Bidirectional Infinite Loop:** Provides seamless, continuous scrolling in both directions.
- [x] **Custom Paging:** Custom snap-to-center behavior without relying on standard `isPagingEnabled`.
- [x] **Custom Layout:** Fully customizable item size and spacing.

## Requirements
- UIKit
- iOS 13+

## 📦 Installation

### Swift Package Manager (SPM)

Add the package URL to your Xcode project's Package Dependencies:

- File > Swift Packages > Add Package Dependency
- Add `https://github.com/EJLee1209/InfiniteCarousel.git`
- Select "Up to Next Major" with "1.0.1"

### CocoaPods

Add the following line to your `Podfile`:

```ruby
pod 'EJInfiniteCarousel'
